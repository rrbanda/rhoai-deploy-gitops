apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: llamastack

resources:
  # Secrets are NOT managed by ArgoCD -- create manually before first sync.
  # See secrets-placeholder.yaml for the templates and instructions.
  - postgres-pvc.yaml
  - postgres-deployment.yaml
  - postgres-service.yaml
