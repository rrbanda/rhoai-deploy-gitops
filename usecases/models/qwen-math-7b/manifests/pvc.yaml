apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: qwen-model-weights
  labels:
    app.kubernetes.io/name: qwen-model-weights
    app.kubernetes.io/component: storage
    app.kubernetes.io/part-of: qwen-math-7b
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: gp3-csi
  resources:
    requests:
      storage: 30Gi
