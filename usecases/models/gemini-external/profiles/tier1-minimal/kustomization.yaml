apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: models-as-a-service

labels:
  - pairs:
      app.kubernetes.io/part-of: gemini-external
      tier: "1"

resources:
  - ../../manifests
