apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: model-weights
  labels:
    app.kubernetes.io/name: model-weights
    app.kubernetes.io/component: storage
    app.kubernetes.io/part-of: orchestrator-8b
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 50Gi
