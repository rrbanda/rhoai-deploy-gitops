# ============================================================
# CUSTOMIZE THIS: Replace all cluster-specific hostnames below
# with your cluster's inference service route.
# Example: <model>-<ns>.apps.<cluster-domain>
# ============================================================
apiVersion: networking.istio.io/v1
kind: ServiceEntry
metadata:
  name: remote-inference-cluster
  namespace: ai-gateway
  labels:
    app.kubernetes.io/part-of: ai-gateway
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  hosts:
    - qwen25-7b-inference-llm-inference.apps.cluster-4l6x6.4l6x6.sandbox1213.opentlc.com
  location: MESH_EXTERNAL
  ports:
    - number: 443
      name: https
      protocol: HTTPS
  resolution: DNS
---
apiVersion: v1
kind: Service
metadata:
  name: inference-backend
  namespace: ai-gateway
  labels:
    app.kubernetes.io/part-of: ai-gateway
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  type: ExternalName
  externalName: qwen25-7b-inference-llm-inference.apps.cluster-4l6x6.4l6x6.sandbox1213.opentlc.com
  ports:
    - port: 443
      targetPort: 443
      protocol: TCP
      name: https
