# ============================================================
# CUSTOMIZE THIS: Replace all cluster-specific hostnames below
# with your cluster's inference service route.
# Example: <model>-<ns>.apps.<cluster-domain>
# ============================================================
apiVersion: networking.istio.io/v1
kind: ServiceEntry
metadata:
  name: remote-inference-cluster
  namespace: ai-gateway
  labels:
    app.kubernetes.io/part-of: ai-gateway
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  hosts:
    - CHANGE_ME.apps.CHANGE_ME_CLUSTER_DOMAIN
  location: MESH_EXTERNAL
  ports:
    - number: 443
      name: https
      protocol: HTTPS
  resolution: DNS
---
apiVersion: v1
kind: Service
metadata:
  name: inference-backend
  namespace: ai-gateway
  labels:
    app.kubernetes.io/part-of: ai-gateway
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  type: ExternalName
  externalName: CHANGE_ME.apps.CHANGE_ME_CLUSTER_DOMAIN
  ports:
    - port: 443
      targetPort: 443
      protocol: TCP
      name: https
