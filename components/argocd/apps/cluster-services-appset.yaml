apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: cluster-services
  namespace: openshift-gitops
spec:
  goTemplate: true
  syncPolicy:
    applicationsSync: sync
  generators:
    - git:
        repoURL: https://github.com/rrbanda/rhoai-deploy-gitops.git
        revision: rhoai-3.4-ea2
        directories:
          - path: usecases/services/*/profiles/tier1-minimal
          # Exclude services not currently deployed (re-enable by removing exclude)
          - path: usecases/services/toolorchestra-app/profiles/tier1-minimal
            exclude: true
          # MaaS services managed by rrbanda/maas-demo via dedicated ArgoCD Application
          - path: usecases/services/ai-gateway/profiles/tier1-minimal
            exclude: true
          - path: usecases/services/guardrails-gateway/profiles/tier1-minimal
            exclude: true
          - path: usecases/services/llm-d-epp/profiles/tier1-minimal
            exclude: true
  template:
    metadata:
      name: 'service-{{ index .path.segments 2 }}'
      namespace: openshift-gitops
    spec:
      project: usecases
      source:
        repoURL: https://github.com/rrbanda/rhoai-deploy-gitops.git
        targetRevision: rhoai-3.4-ea2
        path: '{{ .path.path }}'
      destination:
        server: https://kubernetes.default.svc
      ignoreDifferences:
        - group: serving.kserve.io
          kind: InferenceService
          jsonPointers:
            - /status
            - /metadata/annotations
        - group: serving.kserve.io
          kind: ServingRuntime
          jsonPointers:
            - /status
        - group: route.openshift.io
          kind: Route
          jsonPointers:
            - /spec/host
            - /status
        - group: ""
          kind: Secret
          jsonPointers:
            - /data
            - /stringData
      syncPolicy:
        automated:
          selfHeal: true
          prune: true
        syncOptions:
          - ServerSideApply=true
          - SkipDryRunOnMissingResource=true
          - CreateNamespace=true
          - RespectIgnoreDifferences=true
        retry:
          limit: 10
          backoff:
            duration: 60s
            maxDuration: 10m0s
            factor: 2
