apiVersion: rhdh.redhat.com/v1alpha5
kind: Backstage
metadata:
  name: developer-hub
  namespace: rhdh
  annotations:
    argocd.argoproj.io/sync-wave: "2"
spec:
  application:
    route:
      enabled: true
  database:
    enableLocalDb: true
