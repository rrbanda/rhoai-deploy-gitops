apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# GPU worker node provisioning is cloud-specific.
# This directory intentionally has no default resources.
#
# Choose your cloud provider:
#   AWS:        oc apply -k examples/aws/
#   Other:      Create your own MachineSets or provision GPU nodes manually.
#
# See README.md in this directory for details.
resources: []
