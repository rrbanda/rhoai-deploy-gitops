apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Dev overlay: inherits the full DSC from base with all components enabled.
# Use this for development/test clusters. Customize by adding patches below.

resources:
  - ../../base
  - gateway-memory-rbac.yaml
