apiVersion: kueue.openshift.io/v1
kind: Kueue
metadata:
  name: cluster
  annotations:
    argocd.argoproj.io/sync-wave: "3"
spec:
  managementState: Managed
  config:
    integrations:
      frameworks:
        - BatchJob
        - RayJob
        - RayCluster
        - JobSet
        - PyTorchJob
        - TrainJob
