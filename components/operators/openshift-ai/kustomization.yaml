apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../../vendor/gitops-catalog/openshift-ai

patches:
  - target:
      kind: Subscription
      name: rhods-operator
    path: patch-channel.yaml
  - target:
      kind: Subscription
      name: rhods-operator
    path: patch-source.yaml
  # Uncomment to override OperatorGroup name (for brownfield adoption):
  # - target:
  #     kind: OperatorGroup
  #     name: rhods-operator-group
  #   path: patch-operatorgroup-name.yaml
